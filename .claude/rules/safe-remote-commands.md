# 远程与自动化安全规则

- 只读操作可以直接执行：查看文件、状态、日志、测试输出、分支和 diff。
- 写操作必须和当前任务直接相关，且不覆盖用户未确认的工作。
- 不自动执行 `git push`、发布、部署、删除远端资源或修改系统级配置。
- 不输出、记录或提交 API Key、Token、密码、私钥和 Cookie。
- 脚本应默认不覆盖已有文件；需要覆盖时使用显式 `-Force` 或等价参数。
- 远程 relay 或 headless 指令应原样转述用户任务，不私自加入隐藏目标。
